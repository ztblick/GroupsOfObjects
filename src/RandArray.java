/**
 * RandArray
 *
 * This allows a user to create an array of random ints.
 * They specify the size of the array, as well as the
 * minimum and maximum values in the range of ints.
 *
 * @author ztblick
 *
 * Written on Oct. 19, 2023
 * For CS2 @ Menlo School
 * in Atherton, CA
 */

public class RandArray {

    private int[] nums;
    private int min;
    private int max;

    public RandArray(int size, int min, int max) {
        this.min = min;
        this.max = max;
        nums = new int[size];
    }

    /**
     * Traverse the RandArray and fill it.
     */
    public void fill(){

    }

    /**
     * @return The RandArray in the form "[a, b, c, ..., n]"
     */
    public String toString() {
        String out = "";

        return out;
    }

    public static void main(String[] args) {
        // Create a RandArray

        // Fill it

        // Print it using toString()


    }
}
